{
  "name": "styleguide",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "styleguide:init": "styleguide initialize",
    "styleguide:start": "styleguide start",
    "live-server:start": "live-server",
    "serve": "concurrently --kill-others \"npm run styleguide:start\" \"npm run live-server:start\" "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^3.1.0",
    "devbridge-styleguide": "^0.4.17"
  },
  "dependencies": {
    "live-server": "^1.2.0"
  }
}
